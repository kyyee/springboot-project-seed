#h2 database config
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./h2/sps;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
