#h2 database config
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:./sqlite/seed.db
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true