#mysql database config
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
#    ip 可修改
    url: jdbc:mysql://localhost:3306/seed
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true